# MongoDB
```bash
docker-compose up -d
```

# Backend
```bash
cd backend
npm install
npm run dev
```
Server runs on http://localhost:3000

# Frontend
```bash
cd frontend
npm install
npm run dev
```
Server runs on http://localhost:3030
